/**
 * The user's profile — personal settings that aren't tied to a single audition.
 * Kept small for now; we'll add account fields here in Phase 2.
 */
export interface Profile {
  instrument?: string; // the user's chosen instrument, e.g. "Cello"
}
